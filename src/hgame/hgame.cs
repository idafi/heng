using System;
using heng;

namespace hgame
{
	static class hgame
	{
		static int Main(string[] args)
		{
			CoreTester tester = new CoreTester();
			tester.Test();

			Console.ReadLine();
			return 0;
		}
	};
}